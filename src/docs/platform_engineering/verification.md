---
title: 教材修订与验证范围
icon: /assets/icons/article.svg
order: 93
date: 2026-09-23
---

## 本轮教材重构

当前内容包括 16 章独立长篇正文、35 课入门与衔接正文、14 篇专业专题讲义，以及 14 卷、168 个章节单元的教学设计。168 个单元均已逐项列出 IM 练习与验收目标；设计数量与正文完成数分别记录。

分章设计不代表全部章稿已经完成，正文数量与知识覆盖分别报告。新增讲义包含原创机制推导、反例、业务联系与分层练习。

本轮只做静态内容审阅：本地链接、导航覆盖、课号、代码围栏、示例标识和章节清单。未执行站点构建、未运行示例，也未启动数据库、集群或模型服务。新增代码块与计算示例标为教学过程或预期结果。

案例沿用虚构任务与积分平台。早先调整后的 team-a/team-b 配套示例也没有在本轮运行验证，历史结果不能自动证明当前版本通过。

## 2026-09-23：前两章长篇正文

- [01.01 程序与工具链](./curriculum/01_go/01_program_toolchain.md)：将用户认可的 DeepTutor 审阅稿接入本地课程。
- [01.02 类型与数据表示](./curriculum/01_go/02_types_data.md)：DeepTutor 生成的 7 个正文块全部 ready，逐节审阅后整理为 7 节、9 道分层练习和 12 组边界用例。
- 核对整数溢出、常量转换、浮点格式、字符串字节长度和解析失败规则；补充已有状态校验及相加前的额度检查。
- 更新侧栏、卷内目录、系列入口、学习方法与个人进度模板；原稿和哈希保存在 drafts/platform_engineering。

本次生成调用了现有 DeepTutor 和模型服务，没有启动新的教学服务。HTTP 创建接口的参数兼容问题通过临时 SDK 进程适配绕行，未修改其源码或持久配置。教材示例未编译或运行，站点未构建；生成接口成功、静态审阅与实际运行验证分别报告。

当次接入检查结果：课程目录共 86 个 Markdown 页面，562 个本地链接可解析；侧栏覆盖 86 个唯一页面，无重复或遗漏。14 卷各有 12 个不同的计划单元，共 168 个。两章原稿、审阅稿与课程文件的来源哈希一致，代码围栏与折叠块配对检查通过。

## 2026-09-23：学习路线与项目同步流程

新增[学习路线](./curriculum/learning_path.md)，用 S0–S7 串起主线，AI 单列方向；明确当前可读的前六步、各阶段前置、章节顺序、业务增量与验收。调整相关分卷起步要求，补齐指针、测试与 SQL 的教学位置，并前置基本安全、观测和评测。

仓库 drafts/platform_engineering/authoring_workflow.md 固化每章的项目同步步骤：审阅正文写入课程目录，同时同步导航、进度、内容状态及来源哈希，静态检查后记录交付。两章现有正文已同步，完成数仍为 2/168；本次不将路线设计计为新增章稿。

本次只修改课程文档与侧栏，未生成新的 DeepTutor 章稿、构建站点或运行教学示例。路线审阅依据与未完成内容保存在 drafts/platform_engineering/learning_path_review.md。

检查结果：87 个课程页面、619 个本地课程链接、87 个唯一侧栏页面均通过静态检查。路线中的章号及范围覆盖全部 168 个计划单元，无无效章号或遗漏；这项覆盖检查不代表章稿已经完成。两章来源哈希保持一致，新增文档表格、标题、围栏与折叠结构检查通过。

## 2026-09-23：以 OpenIM 为参照调整主线

比较 OpenIM、WuKongIM、Tinode 的公开项目资料后，选定 OpenIM。固定服务端 v3.8.3-patch.16，对应提交 f6411a8a1a31d3df36f4c2b3ad28481a94141e1f；只读核对选定的网关、发送、转发、存储、推送与同步入口。GitHub API 查询受到限流后，使用公开仓库页面、发布页面与固定源码快照继续核对；Stars 按页面近似显示记录。

新增 IM 项目阅读地图，调整阶段路线、业务地图、14 卷实践目标、数据库与网络先修、验收和生成流程。现有两章基础稿及旧例仍可读，IM 长篇按新路线逐章适配；本次未增加完成章稿。

静态检查通过：88 个课程页面、641 个本地课程链接、88 个唯一侧栏入口；路线仍覆盖全部 168 个计划单元。13 份选定上游文件的哈希与源码快照一致，公开固定源码链接可在快照中定位。教材两章既有来源哈希保持一致。

上游源码只下载到临时目录用于阅读，项目内保留版本和来源清单；未运行 OpenIM、Go 示例或站点构建，也未将公开性能描述当作测试结果。

## 2026-09-23：逐章 IM 目标与 01.03 正文

14 卷共 168 个单元的实践目标已逐项改写，覆盖消息校验、会话历史、长连接、群聊、存储、可靠性、多端同步、运行与架构取舍。各卷理论知识点继续保留，练习目标不能当作已完成正文。

新增 [01.03 控制流与函数](./curriculum/01_go/03_control_functions.md)：DeepTutor 生成 8 个正文块，均为 ready；HTTP 导出后逐节审阅，整理为基础、应用、第二遍深化和综合四层。章内含 12 道带反馈的练习和 12 组综合边界，并对照固定 OpenIM 入口的控制结构。

已同步正式正文、01.02 的下一章入口、卷目录、侧栏、学习路线、进度、来源和前置交接。现有完成章稿为 3/168；前两章主体旧案例继续作为基础补充。

本次只执行文档静态检查，没有执行 Go 示例、Go 编译或 Go 测试，也没有构建站点或部署 OpenIM。技术行为依据规范、固定源码和静态推导审阅，运行结果仍需学习者独立记录。

接入检查通过：89 个课程页面、660 个本地课程链接、89 个唯一侧栏入口；168 个 IM 实践目标与路线章号覆盖一致。新章的 12 个练习折叠块、表格、标题和围栏检查通过，三章来源哈希及已固定的上游文件哈希核对通过。

## 2026-09-23：第一批并行章稿

三个独立写作任务分别完成 01.04 数组/切片/文本、10.01 可验证需求、10.03 Git 状态，主任务统一复核和接入。DeepTutor 正文块分别为 8、7、8，全部 ready，合计 23 块、0 失败；原始导出、审阅稿和来源记录分别保存。

本批纠正和补充了切片共享与扩容、UTF-8 字节边界、需求接受条件与调用前置、坏状态拒绝、Git 暂存与工作区差异、恢复来源和共享历史等说明。三章各含分层练习与反馈，统稿再次核对前置和预期，不将多 agent 审阅当作运行验证。

共享侧栏、卷目录、路线、进度和来源统一更新，完成数为 6/168。未运行 Go 或 Git 教材练习、未构建站点、未部署 OpenIM；本次 Git 命令只用于读取当前变更和静态差异检查，没有提交或推送。

最终静态检查通过：92 个课程页面、699 个本地课程链接、92 个唯一侧栏页面；六章原稿、审阅稿和正式文件的哈希核对一致。新增三章的表格、标题、围栏与共 43 个练习折叠块均通过结构检查，学习路线仍覆盖全部 168 个计划单元。

## 2026-09-23：第二批并行章稿

新增 01.05 map/结构体/指针、02.01 离散对象与成本、03.01 数据/指令/存储层次。三个独立任务实际生成 DeepTutor 正文块 9、8、8，合计 25 块全部 ready，无失败；主任务核对正式正文、练习及与前章的边界后统一接入。

技术审阅覆盖：map 缺失与 nil、指针重绑定、内层共享和外层写回；扇出对象与单位、渐近界、平均和摊还、概率依赖与循环不变量；二进制/十进制单位、文字字节、缓存轨迹和模型预算。纸面数字均说明假设，未当作硬件或上游性能测量。

已更新卷目录、侧栏、路线、进度、环境说明、原稿档案和来源，正文完成数为 9/168。01.04 的下一章入口已指向 01.05，并同步审阅稿与哈希。

本批未运行 Go 示例、测试、基准、系统实验、站点构建或 OpenIM 部署；仅执行文档静态检查、源码阅读与模型推导核对，没有提交或推送。

最终检查通过：95 个课程页面、742 个本地课程链接、95 个唯一侧栏入口；九章来源哈希核对一致。新增三章共 48 个练习折叠块，标题、代码围栏、数学块和表格结构均通过静态检查，路线仍覆盖全部 168 个设计单元。

## 2026-09-23：01.06 方法与接口

DeepTutor 生成 8 个正文块，全部 ready。审阅后接入 [01.06 方法与接口](./curriculum/01_go/06_methods_interfaces.md)：第一遍覆盖方法、接收者、方法集、小接口和 typed nil，第二遍覆盖断言、组合与嵌入；综合案例只提供本地内存存储。

审阅移除了原稿中超出先修范围的锁、`error`、文件存储和未定义类型，统一本地消息身份为会话 ID 加消息 ID，并明确 Put、Find、接口替换、typed nil 和审计包装各自的保证范围。固定 OpenIM 接口只用于核对方法接口与构造返回接口值的局部事实。

本章同步正文、卷目录、侧栏、路线、进度和来源记录，完成数为 10/168。未运行 Go、测试、站点构建或 OpenIM；最终静态检查范围随本轮更新。

最终静态检查通过：96 个课程页面、757 个本地课程链接和 96 个唯一侧栏入口；10 章来源哈希均一致。新章 24 个练习折叠块、标题、代码围栏和表格结构均通过检查，学习路线继续覆盖全部 168 个设计单元。

## 2026-09-23：01.07 错误与资源生命周期

DeepTutor 生成 8 个正文块，全部 ready，并通过 HTTP Markdown 导出核对。审阅后接入 [01.07 错误与资源生命周期](./curriculum/01_go/07_errors_resources.md)：第一遍建立 `error`、哨兵错误、紧邻调用点检查、`defer`、`Reader`/`Writer` 和文件关闭责任；第二遍解释错误包装、`Is`/`As`、关闭错误优先级与 panic/recover 的狭窄边界。

审阅移除了生成器重复的书籍导航、未经解释的 I/O 示例和将普通失败交给 panic 的写法。课程统一使用虚构会话、消息与本机路径；`n > 0` 的读取结果先处理，再解释 `err`；本地 map、文件读取和关闭动作都不被扩张解释为服务端受理、稳定持久化、设备接收或已读。

本章同步正文、01.06 的下一章链接、卷目录、侧栏、路线、进度和来源记录，完成数为 11/168。未运行 Go、测试、站点构建或 OpenIM；标准库和语言规范是本章技术事实来源，未新增 OpenIM 实现结论。

最终静态检查通过：97 个课程页面、770 个本地课程链接和 97 个唯一侧栏入口；11 章来源哈希均一致。新章有 20 个练习折叠块、21 组代码围栏、1 个主标题和 10 个二级标题；14 卷仍各有 12 个计划单元，总计 168 个。

## 2026-09-23：01.08 常用标准库协作

DeepTutor 生成 8 个正文块，全部 ready，并通过 HTTP Markdown 导出核对。审阅后接入 [01.08 标准库协作](./curriculum/01_go/08_standard_library.md)：从文件格式、JSON tag、解码后验证、RFC3339、逐行扫描、读取上限、覆盖写入和 flag 输入依次展开。

审阅删除了重复导览和把普通编码失败交给 panic 的示例；严格区分 JSON 解码、业务验证、读入大小、覆盖写入和崩溃安全边界。本章只依赖 Go 标准库文档，不新增 OpenIM、HTTP、网络、并发或消息交付结论。

最终静态检查通过：98 个课程页面、779 个本地课程链接和 98 个唯一侧栏入口；12 章来源哈希均一致。14 卷仍各有 12 个计划单元，总计 168 个。未执行 Go、测试、站点构建或 OpenIM。

## 2026-09-24：01.09 包设计与依赖演进

DeepTutor 生成 8 个正文块，全部 ready，HTTP Markdown 导出已核对。审阅后接入 [01.09 包设计与依赖演进](./curriculum/01_go/09_packages_evolution.md)：先解释目录、包、导入路径和模块，再用 `history`、`internal/historyfile`、`cmd/historytool` 三包示例讲公开 API、依赖方向、导入环、`internal` 和兼容演进。

审阅把原稿中多套不一致的类型、包路径与方法签名统一为同一条虚构 IM 历史案例，修正了私有字段无法直接 JSON 编码、文件版本与模块版本混淆等问题。正文有 20 道分层练习；固定 OpenIM 接口只用于后续源码阅读问题，本章不新增其运行或可靠性结论。

最终静态检查通过：99 个课程页面、795 个本地课程链接和 99 个唯一侧栏入口；13 章来源哈希均一致，14 卷各有 12 个计划单元，总计 168 个。未执行 Go、测试、站点构建或 OpenIM。

## 2026-09-24：10.02 测试基本方法

DeepTutor 生成 8 个正文块，全部 ready，HTTP Markdown 导出已核对。审阅后接入 [10.02 测试基本方法](./curriculum/10_engineering/02_testing_basics.md)：沿 10.01 的决策表建立独立预期，再解释 `_test.go`、`testing.T`、失败报告、表驱动、子测试、拒绝后状态断言、临时文件集成和覆盖率边界。

原稿对空正文、无效上限和分类优先级有冲突，并将新建错误的文字当作错误身份；审阅稿统一采用 10.01 规则，分别核对 `(bool,string)` 校验结果与 01.09 的哨兵错误。所有 `go test` 命令与 FAIL 输出均为教学说明或假想结果，固定 OpenIM 入口仅用于提出局部源码问题。

最终静态检查通过：100 个课程页面、811 个本地课程链接和 100 个唯一侧栏入口；14 章来源哈希均一致，14 卷各有 12 个计划单元，总计 168 个。新章 20 道折叠练习、8 组代码围栏结构通过；未执行 Go 测试、示例、站点构建或 OpenIM。

## 2026-09-24：02.02 线性结构

DeepTutor 生成 8 个正文块，全部 ready，HTTP Markdown 导出已核对。审阅后接入 [02.02 线性结构](./curriculum/02_algorithms/02_linear_structures.md)：从任务身份与位置、切片保留、链表头尾、栈与 FIFO 进入固定容量环形队列，再用容量 4 的状态表核对回绕、满空、清零和拒绝。

审阅统一原稿多次重复的切片、链表和环形队列解释；给 `append` 的摊还成本加上增长策略前提，区分“旧槽引用仍在”与“底层数组仍被切片引用”，并明确满时由上层承担业务处理。本章不对 OpenIM 队列实现、网络发送或消息交付作结论。

最终静态检查通过：101 个课程页面、814 个本地课程链接和 101 个唯一侧栏入口；15 章来源哈希均一致，14 卷各有 12 个计划单元，总计 168 个。新章 20 道折叠练习、6 组代码围栏结构通过；未执行 Go、测试、站点构建或 OpenIM。

## 2026-09-24：02.03 搜索与循环不变量

DeepTutor 生成 8 个正文块，全部 ready，HTTP Markdown 导出已核对。审阅后接入 [02.03 搜索与循环不变量](./curriculum/02_algorithms/03_search_invariants.md)：先用无序切片顺序扫描建立“首次满足”的含义，再增加排序与单调谓词前提，推导下界、上界、重复键、`sort.Search` 和补拉候选位置。

原稿把未分类数组位置 `[lo,hi)` 与可等于 `hi` 的答案边界混淆；审阅稿明确答案在 `[lo,hi]`，分别按初始化、保持、终止说明正确性。还补充“同键只处理部分消息时，上界会跳过未见记录”的业务反例；固定 OpenIM 同步文件只作为后续阅读入口，不新增协议保证结论。

最终静态检查通过：102 个课程页面、828 个本地课程链接和 102 个唯一侧栏入口；16 章来源哈希均一致，14 卷各有 12 个计划单元，总计 168 个。新章 20 道折叠练习、4 组代码围栏结构通过；未执行 Go、测试、站点构建或 OpenIM。

## 首版历史记录

首版课程在 2026-09-22 留有以下运行记录，仅表示当时版本的检查范围，不能自动沿用为本轮修订后的运行结论：

| 首版检查 | 当时记录的结果 |
|---|---|
| VuePress 构建与链接 | 页面、导航、交互组件构建和本地链接检查通过 |
| Go | 原有契约、并发、重放、fencing、取消、帧解析、HTTP 测试与 race、vet 通过 |
| HTTP | 当时进行了本机请求与 SIGTERM 退出验证 |
| MySQL/Redis | 原有事务、并发去重、冲突、回滚和缓存基础命令进行了容器验证 |
| Python | 原有 10 项测试与 8 条固定检索案例通过，无模型调用 |
| 排队模型 | 原有 4 项纯模型检查通过 |

首版 Go 运行环境为 1.25.6，声明最低版本为 1.24；不代表验证过所有版本与操作系统。

## 学习时仍需独立验证

- A、B 篇个人练习程序，以及整合账户、索引与缓存练习的实际输出。
- 两会话锁等待、独立变式、业务领域唯一性和综合项目集成。
- Kubernetes 部署与恢复、镜像运行和真实模型接入。
- 真实多进程、网络故障与外部工具效果，不能由单进程模型推断。
- 网页交互的浏览器表现，不能由 Markdown 或纯模型检查替代。

将自己的版本、命令、结果和限制填写到[学习记录](./progress.md)，分别标明理论推导、参考示例运行与独立业务验证。
