---
title: 课程交付验证记录
icon: /assets/icons/article.svg
order: 93
date: 2026-09-22
---

本页记录教材与参考实验的工程验证，不代表学习者已经掌握课程。验证日期为 2026-09-22。

## 已执行

| 检查 | 结果与范围 |
|---|---|
| VuePress 站点构建 | 页面、导航、交互组件成功编译与静态渲染 |
| 本地 Markdown 链接 | 专题所有本地相对链接存在，代码围栏配对 |
| Go 测试与 race | 契约、所有权、并发重复、参数冲突、租户、回执、outbox 重放、fencing、取消、帧解析、HTTP 边界通过 |
| Go vet | 通过 |
| HTTP 端到端 | 临时本机端口实测正常请求、重复、冲突、跨租户拒绝、SIGTERM 退出 |
| MySQL 8.4 | 容器实测重复、并发去重、参数冲突、租户隔离、中途 outbox 写失败后的事务回滚 |
| SQL 执行计划 | 余额查询使用复合主键，单行定位 |
| Redis 7.4 | 在独立容器验证虚构键写入、过期参数与读取 |
| Python 标准库实验 | 10 项测试通过，包括权限前置、文件重开后的恢复和重复效果控制 |
| 固定检索评测 | 8 条固定案例通过；无模型调用，不代表真实问答质量 |
| 网页排队模型 | 4 项 Node 测试通过，覆盖平衡负载、持续过载、任务守恒与非法输入 |
| 合成延迟示例 | 1000 样本，均值 14.45ms、P99 5ms、最大 5000ms，与正文一致 |

Go 验证环境为 1.25.6，代码声明最低 Go 1.24；没有声称已在所有支持版本和操作系统验证。实验容器完成后停止，虚构数据库数据保留在课程专用 volume。

## 条件实验与学习者任务

- Kubernetes 清单与运行步骤已提供，但本轮未创建集群、未执行发布演练。
- Dockerfile 已提供，站点构建与 Go 编译不等于镜像构建验证；本轮未构建课程服务镜像。
- 未接入真实 LLM、embedding 或外部工单系统；相应评测需要个人测试环境。
- 两个交互 MySQL 会话的锁等待、额外造数、独立变式与综合项目由学习者按正文完成；不会因参考测试通过自动记为完成。
- 网页组件已通过生产构建与纯模型测试，本轮未使用浏览器自动化执行点击。

如果之后调整代码、依赖或示例，应重跑对应检查并更新日期与范围。请把自己的完成记录放在[学习进度](./progress.md)的个人副本中。
