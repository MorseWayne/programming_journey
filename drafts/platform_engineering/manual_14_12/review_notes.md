# 14.12 审阅记录：可持续交付

## 来源与生成状态

- 2026-09-25 请求 DeepTutor BookEngine 生成八节中文深度课程；ideation 阶段五次 HTTP 502，90 秒后停止，没有 book/page/原稿。
- 本目录保存人工编写并审阅的静态章稿与生成请求；没有 `original.md`，不声称 DeepTutor 已生成。模型端点恢复后补原稿并审阅差异。

## 单章覆盖与教学审阅

- 用虚构 release manifest 分列 IM 合同、current/proposed 资料、权限、索引、提示/回答契约、模型/适配器和固定题集，避免只说“模型 v2”而失去可追溯性。
- 14.09 六题用于回归，不伪称未见集；`q-03` 越权、`q-01` 当前 6 B 和 R9 仍待审为独立发布硬门。
- 离线、影子、灰度逐层扩大；影子不展示结果仍须解决真实私聊的授权、隔离和保留，课程未运行任何灰度。
- 用户负反馈需有权审阅、版本化与按桶归因，说明自选择偏差及测试集磨损；不能直接变金标签或自动训练数据。
- 回滚必须考虑资料、索引、权限、答案/前缀缓存、提示、模型与输出兼容；回滚不能撤销已泄露内容，也不能把未来获批 R9 简单退成旧规则。
- 用 R9 被误标 current 导致 `q-01` 答 9 B 的纸上事故，按遏制、核影响、修复验证、复盘顺序训练第一坏边界判断。22 题分版本、灰度反馈、事故决策三层。

## 权威核对与边界

- Google ML 原始部署测试/监控、Google SRE 灰度/事件与发布工程、MLflow 官方版本机制、OWASP RAG 原始安全文档用于概念核对；未声称课程真的使用这些产品。
- 没有模型、IM、数据库、站点、发布、灰度或事故运行，也没有生产阈值和真实用户反馈；所有 release/结果为虚构纸上例子。
- 第十四卷十二章独立正文完成，剩余章节在 Go/算法/网络卷，继续按先修关系推进。
