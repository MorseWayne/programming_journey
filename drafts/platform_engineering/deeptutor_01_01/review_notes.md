# DeepTutor 第一章试生成与审阅记录

## 实际结果

- DeepTutor BookEngine 成功生成一章正文和一个自动导览。
- 正文 7 个内容块全部为 ready，没有失败块。
- 已通过 DeepTutor HTTP 导出接口取得 Markdown，与 Python 导出结果一致。
- 原稿保持在 original.md；reviewed.md 为经技术和叙事审阅的版本。

## 生成链路的限制

HTTP 创建接口在本次重试中返回参数冲突：max_tokens 与 max_completion_tokens 同时存在且数值不一致。此次通过 DeepTutor 的 Python 入口生成，仅在这个进程的提供者调用中规范化参数。没有修改 DeepTutor 项目源码或持久模型配置，常规 HTTP 创建接口的兼容问题仍待单独修复。

## 审阅修正

1. 去掉自动导览的空概念图、原始生成要求和重复索引，保留真正教学正文。
2. 清理相邻的重复小节标题，减少反复解释相同操作的段落。
3. 将模块路径统一为 example.com/arena-hello，修正原稿中不允许的末尾斜杠。
4. 说明 main.go 是文件命名习惯，真正入口由 main 包和 main 函数决定。
5. 说明程序先初始化，再调用 main；入口 main 的签名不含参数和返回值。
6. 将 go run .、文件列表运行、go build 的作用分开；补充临时编译与项目目录的关系。
7. 修正“必须在源文件目录运行”的绝对说法，保留使用显式相对路径的情况。
8. 区分 PowerShell、Windows cmd 与类 Unix 终端的目录查看命令。
9. 修正把非 main 包一概归为语法错误的说法，以及“运行时自动格式化”的误导。
10. 将原稿中被写成逐行行内代码的完整程序整理为代码围栏，明确故意错误例子和完整程序的区别。
11. 统一虚构任务和用户措辞，输出作为学习时的预期，而非声称本次已经运行。

## 核对依据与验证范围

- [Go 模块路径](https://go.dev/ref/mod#module-paths-and-versions)：模块路径不得以斜杠开头或结尾。
- [Go 程序执行](https://go.dev/ref/spec#Program_execution)：初始化与入口 main 的规则。
- 已检查 Markdown 围栏、重复标题、无效模块路径、虚构背景和来源哈希。
- 没有执行站点构建、Go 编译或运行练习；生成成功不能代替示例运行验证。

## 状态

用户认可试讲质量后，已将审阅稿接入本地课程 01.01 页面。原稿与审阅记录继续保留在 drafts；本次接入没有执行站点构建或推送。
