# 02.10 审阅记录：动态规划与贪心

## 来源与生成状态

- 2026-09-25 请求 DeepTutor BookEngine 生成八节中文深度课程；ideation 阶段五次 HTTP 502，90 秒后停止，没有 book/page/原稿。
- 本目录保存人工编写、审阅的静态算法章稿与生成请求；无 `original.md`，不声称 DeepTutor 已生成。端点恢复后补原稿并对账。

## 单章覆盖与教学审阅

- 用预算 M=4、A(3,5)、B(2,3)、C(2,3) 穷举合法组合；B+C 值 6 最优，按价值/消耗先 A 只得 5，具体反驳该 0/1 背包贪心规则。
- `dp[i][b]` 先定义语义与边界，再用 `dp[i-1]` 的取/不取转移填四行表、追溯 B+C；说明重叠子问题和最优子结构的前提。
- 一维倒序更新保持每项一次；只处理 B 时正序会把预算 4 错算成 6。`O(nM)` 的 M 是**数值预算**而非输入长度，解释伪多项式及大预算问题。
- 单槽、等价值、不重叠区间 A=[1,4)、B=[2,3)、C=[3,5) 以最早结束选 B+C 两项，交换论证只保证这一精确定义，不扩张到带权/多槽或 IM 权限任务。
- 预算/价值为人工纸上单位，不得抵消当前正文 6 UTF-8 B、重复 ID 409、成员权限或把 `accepted_in_memory` 称设备送达。22 题分模型、递推、业务决策三层。

## 权威核对与边界

- MIT 6.006 官方背包/复杂度讲义与 6.046 官方区间调度讲义用于递推、伪多项式和交换论证核对。
- 独立穷举和递推算术复核 `dp` 行：`[0,0,0,0,0]`、`[0,0,0,5,5]`、`[0,0,3,5,5]`、`[0,0,3,5,6]`。未运行 Go、调度器、IM 或基准。
- 下一章 02.11 进入 Trie、位图、Bloom filter 和并查集的专用结构。
