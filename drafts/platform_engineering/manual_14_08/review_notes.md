# 14.08 审阅记录：工具、工作流与 Agent

## 来源与生成状态

- 2026-09-25 请求 DeepTutor BookEngine 生成八节中文深度课程；ideation 阶段模型端点五次返回 HTTP 502，90 秒后停止，没有 book/page/原稿。
- 本目录为人工编写、审阅的静态章稿及请求归档；无 `original.md`，不声称 DeepTutor 已生成。端点恢复后须独立生成原稿并审阅差异。

## 单章覆盖与教学审阅

- 从只读资料回答、固定工作流与动态 Agent 的区别进入，再引入工具契约、计划、有限状态、审批、预算和检查点，避免初学者把模型说“已发送”当作副作用。
- 用虚构 `u-a` 向 `c-a` 发 `公告`（两个汉字共 6 UTF-8 B）连接当前 S2 正文上限；`公告9` 为 7 B。批准绑定 actor/会话/正文摘要/动作/有效期，是**本章设计**而非当前 `/v1` 字段。
- 200 只到 `accepted_in_memory`，同 ID 即使同内容也回 409；调用中响应丢失进入 `result_unknown`，不能把 409 或本地无成功记录当幂等成功。未来操作 ID/持久查询是**新协议提案**，不改现行 S2/S3 描述。
- 区分检查点与外部写动作之间的两种崩溃空隙；纸上状态机和故障表明确何时停、何时查证、何时重批。
- 预算/批准由可信应用执行；文档、工具结果和模型计划均不能提升权限或使待审 R9 生效。22 题按概念、失败恢复和权限/验收决策分层。

## 权威核对与边界

- Anthropic 原始工程文章用于 workflow/Agent 区别与停止条件；OWASP 原始 Agent 安全文档用于工具作用域与批准；LangGraph 官方文档仅作 checkpoint/暂停恢复的实现例；RFC 9110 用于 HTTP 方法幂等概念，不能据此推 IM 自定义写接口已幂等。
- 未运行模型、工具、IM、Go、数据库或站点。纸上步数、30 秒和批准流程不是生产配置或现有系统行为。
- 后续 14.10 比较模型选择、批量、缓存、路由、流式与过载；14.11 为按需进阶。
