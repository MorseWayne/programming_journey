# 14.06 审阅记录：检索与索引

## 来源与生成状态

- 2026-09-25 对 DeepTutor BookEngine 发起八节中文深度课程请求；本地环境可用，但模型端点在 ideation 阶段五次返回 HTTP 502，90 秒后停止。未生成 book/page/原稿。
- 本目录保存人工编写和审阅后的静态章稿、生成请求与来源散列；没有 `original.md`，不把本章称为 DeepTutor 已生成。端点恢复后补生成、比对并保留人工审阅改动。

## 单章覆盖与教学审阅

- 从 `q-01/q-02/q-03` 的现行、提议、权限差异进入，再解释资料单位、切分片段、位置与元数据，避免一上来只讲向量库名词。
- 用人工 posting list 教倒排索引、交并集与位置表；BM25 先讲稀有词、词频饱和和长度归一化，不把分数当现行事实。
- 用 `(1,0)`、`(0.8,0.6)`、`(1,0)` 手算余弦 0.8/1.0，展示更近的 R9 仍为 proposed；再区分精确邻居与 ANN/HNSW 的召回、延迟、空间和建索引取舍。
- 用词项/向量相反名次手算 RRF `1/61+1/62≈0.03252`，说明打平并未解决版本问题；重排只能处理已召回候选。
- 说明查询前权限过滤、后过滤的候选饥饿、交付前权威复核与索引/缓存失效。`q-03` 私有正文不进入 `u-b` 的可见候选或模型。
- 将 14.09 的 Recall@k 金证据分母、引用支撑与业务硬门分开，22 题分基础、推演、决策三层。

## 权威核对与边界

- Stanford《信息检索导论》原书用于分词、倒排、位置表及 BM25；Faiss 官方资料用于固定维度向量和索引取舍；HNSW 原论文用于分层图；Elasticsearch 官方文档用于 RRF 公式与其产品的前/后过滤语义。
- 纸上余弦/RRF 算式需逐项复核；没有运行索引、模型、Go/IM、数据库或站点。具体引擎的过滤/召回行为不能由纸上模型推为通则。
- 14.07 才系统讲上下文组装和生成引用，本章交接可追溯、有权、带状态的候选片段。
