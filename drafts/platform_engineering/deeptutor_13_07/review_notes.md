# 13.07 审阅记录：设计说明与评审

DeepTutor BookEngine 八节初稿均 ready、无失败块；本地 HTTP Markdown 导出与归档的 `original.md` SHA256 一致。正式页沿虚构 A 发送、B 离线 25h 的目标组织现状、候选、数据/失败流、容量与验证、评审阻断项和一则 proposed ADR，附 22 道分层练习。

## 教学重组

- 先区分问题简报/设计说明、ADR、实现任务和运行记录的证据责任，避免文档完成被当作功能验收。
- 固定 `/v1` 6 B/4096 B/409/404/`accepted_in_memory`，把 S3 `/v2`、B 25h、成员历史规则、A/B 候选时延和 R9 6→9 B 各写成不同状态。
- 并列评审维持 S2、未来教学 DB 权威与仅延长 broker/增网关三个选项；以 A 确认点、B 有权历史、事件缺口、旧端和运行代价对照。
- 用 DB 已存 E9 未发、E9 重投、A 超时未知、B 见 9 缺 8、非成员历史请求和旧端 7 B 六类反例检验图上的每条箭头。
- 把 13.05 的同口径纸上容量、未来隔离验证、发布/数据/客户端多轴回退与产品/客户端/Go/DB/安全/值班不同评审职责连接到 proposed ADR。

## 技术修订

- 初稿导出有重复 `###` 标题和大量同义重述；正式页重排为设计产物→上下文→候选→状态/故障→容量验证→角色评审→ADR。
- 初稿中段把 S2 写成“在线投递与短期重试”、甚至写过 S2 持久化/已有恢复游标；正式页始终限定 S2 当前只承诺本进程内存受理。
- 初稿后段把 S2/S3 改写为增量恢复与快照恢复方案、把 R9 改写为“恢复游标”或“不可恢复状态”，还把 D1/K1 改写为设备/会话键，严重冲突本系列固定词汇。正式页保持 S3 为未来教学 DB 存储提议、R9 为 6→9 B 待审、D/K 为制品/配置版本。
- 初稿局部建议未来 S3“仅灰度双写”并宣称可一键切回 S2，未证明权威写入、回填和旧端兼容。正式页只列迁移/回退证据门，不把双写或切回当现成能力。
- 初稿在某节将固定 OpenIM 两处源码泛化为“队列、协程或回调、下游推送”；正式页只说选定 `send.go` 调用 `MsgToMQ` 后返回、另一 Mongo 消费路径调用 `BatchInsertChat2DB`，不推完整事务/ACK/架构。
- ADR 示例始终为 **proposed**；正式页明确未批准、未实施、未验证，接受设计与交付验收是两件事。

## 静态边界与同步

- 没有运行 Go、IM、数据库、压测、部署或站点；所有容量、目标与评审都是纸上示例。
- 同步正式页、13.06 下一章链接、第十三卷目录、总目录、侧边栏、学习路线、能力验收与来源散列。
