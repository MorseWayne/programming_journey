# 14.09 审阅记录：评测与数据工程

DeepTutor BookEngine 八节初稿均 ready、无失败块；本地 HTTP Markdown 导出与归档的 `original.md` SHA256 一致。正式页用完全虚构的四份资料和六类 IM 问题，按两遍学习法先建立权限/版本过滤的关键词基线，再讲检索、引用、正确拒答、端到端效果与自动裁判偏差，附 22 道分层练习。

## 教学重组

- 第一遍读 1–4 节：固定 `doc-current/doc-r9/doc-private/doc-history` 的 scope/status，与 `q-01…q-06` 的 actor、可答/拒答标签、金证据和待定业务规则；先授权与版本过滤再关键词排序。
- `q-03` 仅依据 `scope=u-a-only` 对 `u-b` 做不泄正文/存在细节的拒绝；`q-04` 是退群历史规则未定；`q-06` 可确定“不能**仅凭**24h broker 覆盖25h”，未来 DB 补拉条件另待审。
- 开发与最终集按会话/文档/时间审近重复与版本泄漏；六题是教学覆盖桶，不能代表真实用户分布或产品准确率。
- 第二遍读 5–7 节：权限违反独立硬门；四个有预定证据的问题纸上 top2 完整命中三题，`Recall@2=3/4`；另题 top2 仅一相关，`Precision@2=1/2`，均明确**未运行**。
- 端到端按现行事实、提议、无权、规则未定、跨确认点和长离线分桶，正确拒答算成功；裁判偏差需换序/盲化和人工抽检。

## 技术修订

- 初稿导出有重复 `###` 标题和多处同义重述；正式页依第一次数据/基线、第二次组件/产品评测重排，并把两遍阅读入口写在正文。
- 初稿后段把 S2 改称订单“处理中/已出库”状态、把 R9 写成退款或泛化业务拒绝，偏离本系列 S2 当前 IM 内存受理、R9 6→9 B 待审。正式页始终使用固定含义和 09.02 合同。
- 初稿一处把 `q-06` 列为“无法确定/应拒答”，但本题明确问的是**是否只靠 24h broker 覆盖25h**，可以有证据地回答“不能”，并附未来 DB/权限条件。正式页将它放有金证据的可答桶，不把未来完整恢复目标误当现有事实。
- 初稿的 `Recall@2` 部分只说“命中 gold”而没固定 `q-02` 同时需要现行与拟议两份资料，易把只检出 `doc-r9` 算成功。正式页定义“所需最小证据集**全部**在 top2 才算该题命中”。
- 初稿偶尔把 `200` 简写成“请求成功”而遗漏 `accepted_in_memory` 的限定；正式页在 q05 与现行合同处均限定为本进程内存受理，不推 DB/设备结果。
- 引用 Google ML Crash Course 的划分/监测与 ARES、LLM-as-a-Judge 原始研究只作方法来源，不把自动裁判分数或纸上指标宣称为已执行结果。

## 静态边界与同步

- 没有运行 Python、Go、IM、检索、模型、评测或站点；文档、排名、标签和所有结果均为虚构纸上示意。
- 同步正式页、14.03 下一章链接、第十四卷目录、总目录、侧边栏、学习路线、能力验收与来源散列。
