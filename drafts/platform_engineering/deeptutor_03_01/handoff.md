# 03.01 交接

## 生成与正文

- DeepTutor：bk_8b4ae38436 / pg_378c5bc6dc，8 块全部 ready，0 失败；HTTP 原稿 38493 字符。
- 正式页：[03.01 数据、指令与存储层次](../../../src/docs/platform_engineering/curriculum/03_systems/01_data_instructions_storage.md)。
- 八节、16 道反馈题、4 组模型容量边界、两种纸上缓存轨迹与三个完整 Go 示例。
- original / reviewed / generation_request / review_notes / provenance / handoff 全部保留。

## 稳定前置

只依赖 01.01–01.04。没有依赖同批 01.05 的 map、结构体与指针，也没有预设读过数学卷。章节自身定义位权、幂、单位、百分比、速率与整数预算除法。

## 已讲概念

- bit、B、kB/MB 与 KiB/MiB、bit/s 与 B/s，数据量/速率/时间。
- 二进制与十六进制，整数位宽/二补码、浮点范围与精度，UTF-8 字节，第二遍大小端。
- 正文逻辑字节、教学编码封装、元素槽位、容量、共享与独立副本、RSS 口径。
- 地址空间、虚拟地址、物理映射的直觉；名称、值、位置与业务身份。
- 源码、编译、链接、机器指令与运行；len 读取长度与真正扫描正文的区别。
- 寄存器、CPU 缓存、DRAM、持久设备，容量/延迟/带宽/易失性。
- 时间与空间局部性、工作集、假设明确的 LRU 模型，不推导通用性能倍数。
- N×(P+H)×C 模型、每份独立的必要前提、平均与上限、积压账本、计量函数与业务校验的区别。

## 后续不能视为已掌握

真实页表、TLB、缺页、系统调用、线程、调度、分配器布局、GC 机制、实际硬件缓存参数、基准测试、fsync 完整契约、持久化协议、网络交付与 OpenIM 全链路。上述内容只给定位或预告。

## 业务与来源边界

现有原始文本校验规则未改。256 B/64 B/两份副本及 32 MiB 目标是独立预算题，非 OpenIM 参数。恢复、持久、设备收到与已读不互相替代。

只实际核对固定 OpenIM sync_msg.go:GetSeqMessage 的批量获取与 append 累计，未检查入口限制、底层分配或性能。未依赖复杂上游语法作为入门题。

## 公共接入交给主代理

- 第三卷 README 与 sidebar 增加正式页。
- 路线依赖可定位在 01.04 后，不依赖未完成同批章。
- 进度、概念图、assessment、系列数量与 drafts 档案统一更新。
- 计划总数仍为 168，完成章数仅在统稿和接入检查后加 1。
- 统稿修改时同时维护 reviewed 与正式页，并刷新 provenance 哈希。

全部结果是静态预期和算术推演；未运行 Go、系统实验、教学服务或站点构建。

## 统稿接入记录

2026-09-23：卷目录、侧栏、学习路线、进度、档案和来源状态已由主 agent 统一接入；文档静态检查通过，示例与系统实验未运行。
