# 14.07 审阅记录：RAG 完整链路

## 来源与生成状态

- 2026-09-25 请求 DeepTutor BookEngine 生成八节中文深度课程；ideation 阶段模型端点五次返回 HTTP 502，90 秒后停止，没有 book/page/原稿。
- 本目录保存人工编写、审阅的静态章稿和请求。`reviewed.md` 与正式页同步；没有 `original.md`，不声称 DeepTutor 已生成。恢复后须生成原稿、逐段比对并保留已有审阅结果。

## 单章覆盖与教学审阅

- 承接 14.05 的 JSON 契约、14.06 的有权候选和 14.09 的六题/基线，按 actor/意图→检索→权威原文→证据充分性→上下文→生成→逐主张核引用→合法结果的顺序讲。
- 把 `q-01` 当前 6 B、`q-02` R9 提议未生效、`q-03` 无权且模型前拒绝、`q-04` 规则未定分开，避免“全部送模型生成一个答案”的误导。
- 复用 14.03 32-token **玩具预算** `32−6−18−4=4`，强调真实 tokenizer/接口另计；证据片段不得只保留 9 B 而丢“尚待审”。
- 用“两句答案一对一错”的案例说明引用 ID 非空不等于整段忠实；关键主张逐项回读来源和版本，自动核证仍有误差。
- 权限拒绝、规则未定、现行证据暂缺是三种不同停止原因；格式重试不得绕过授权或制造缺失证据。
- 把资料获批/撤权、索引/答案/提示/KV 缓存版本与注入按来源链解释，保留最早失败层诊断。22 题按入口、推演、决策分层。

## 权威核对与边界

- Lewis 等 RAG 原论文只支撑检索与生成结合的基本机制；ARES 原论文支持将上下文相关、回答忠实和回答相关分开评估；OWASP 原始 RAG 安全文档支持入库至输出的权限/注入边界；Google Cloud 官方文档用于 grounding 核查概念。
- 全章无模型、检索、Go/IM、数据库或站点运行；没有真实效果/成本数据。纸上诊断与缓存策略是教学设计，不代表某个 OpenIM 实现。
- 学完后先回访 14.09 第二遍，再进入 14.08 工具与副作用工作流。
