# 13.11 审阅记录：复杂案例答辩

DeepTutor BookEngine 八节初稿均 ready、无失败块；本地 HTTP Markdown 导出与归档的 `original.md` SHA256 一致。正式页将虚构热群 50→500、B 离线 25h、旧 `/v1` 客户端和 Node 故障放在同一答辩题中，训练约束重述、单位重算、扇出候选、失败/回退与证据等级，附 22 道分层练习。

## 教学重组

- 先把当前 `/v1` 6 B/409/404/200 内存受理、未来 `/v2` 仍 6 B 的 stored 提议、R9 待审和 B25h/权限/留存待定分成四类。
- 沿 13.05 基线重算 20 入站/s→未来 2,000 设备任务/s、群 500 时 20,000/s、单热群占 10 入站/s 时 10,000/s；不同情景不乱乘。
- 比较写时派生、读时补拉和混合策略的写/读放大、热点、权限/顺序、失败和维护成本；不宣布某一策略已实现或必然最优。
- 把 B25h>broker24h、旧端 7 B 拒绝、DB 已存 E9 未发、设备缺 8 游标停 7、Pod/Node N−1 与 12,000 积压/20 分钟清空分开推。
- 最后按现行合同、固定源码、纸上计算、待做实验/待决定规则四类证据做可反驳答辩。

## 技术修订

- 初稿导出有重复 `###` 标题及大量同义重述；正式页重排为事实→负载→扇出→长离线/旧端→故障→候选→答辩法。
- 初稿后段把“旧端 7B”当旧版客户端协议、把新端也称 7B，并建议把 7B 降级成 6B。R9 尚未批准，7 B 在当前 `/v1` 和拟议 `/v2` 都超过正文上限；正式页只把 7 B 当应拒绝的字节反例。
- 初稿多处把 S2/S3/R9 改写为风险/事故等级或旧端数据格式；正式页始终保留 S2 当前内存受理、S3 未来教学 DB、R9 6→9 B 待审的全系列含义。
- 初稿将 B25h 与 broker24h 的差 1h 写成可能违约或可接受延迟，但并无已批准的 25h 历史合同。正式页只得出 broker 不能覆盖全窗口，并将权威留存与历史可见列待定门。
- 初稿把 312.5 MiB 连接驻留分量写成“单份数据”，并把混合派生说成更稳妥的定论；正式页分别记连接和权威历史账，保留候选策略及实验条件。
- 初稿有“OpenIM 两处源码证明先写库再异步消费”等越界推断；正式页只说所读 `send.go` 选定分支调用 `MsgToMQ` 后返回，另一 Mongo 消费路径调用 `BatchInsertChat2DB`，不能证明写时/读时扇出或容量。

## 静态边界与同步

- 没有运行 Go、IM、数据库、压测、部署或站点；所有故障、策略和分数都是纸上答辩材料。
- 同步正式页、13.10 下一章链接、第十三卷目录、总目录、侧边栏、学习路线、能力验收与来源散列。
