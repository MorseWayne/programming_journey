# 02.09 审阅记录：加权路径与状态空间

## 来源与生成状态

- 2026-09-25 请求 DeepTutor BookEngine 生成八节中文深度课程；ideation 阶段五次 HTTP 502，90 秒后停止，没有 book/page/原稿。
- 本目录保存人工编写和审阅的静态算法章稿及生成请求；无 `original.md`，不声称 DeepTutor 已生成。端点恢复后须生成原稿并逐段对账。

## 单章覆盖与教学审阅

- 承接 02.08 的无权 BFS/DFS 与 02.07 堆，先定义同单位、可加的静态边权和最短路径目标，再用一张虚构有向图对比最少 2 跳/代价 12 与最低代价 4 跳/代价 7。
- 以 `dist/parent`、松弛、最小堆和定型表逐行解释非负权 Dijkstra，强调 E **有效出堆**可停、首次入堆 10 不可停、旧 C(5) 条目要跳过。
- 给无环负权反例 `S→A=2,S→B=5,B→A=−4`，真值 A=1；明确失败的是“出堆后只定型一次”的 Dijkstra 前提，不泛化为所有重开顶点实现都必错。
- F 孤立不可达，Go map 缺键 0 不可当 ∞，整数累加须防溢出；Bellman–Ford/DAG 路线按负权、负环和拓扑前提说明。
- “最多一次受限 broker 边”扩成 `(vertex,usedBroker)` 状态，不能只按顶点 visited；路径算法不授予 IM 权限，不证明 S2 200 后设备送达。
- 纸上全路径枚举复核最少跳/最低代价与负权算式；22 题按单位/路径、松弛/前提、状态/业务三层组织。

## 权威核对与边界

- Princeton《Algorithms》作者教材原站、MIT 6.006 与 Go `container/heap` 官方文档用于最短路径/非负前提、负权与堆语义；没有运行 Go、IM 或基准。
- 图中的代价 2/5/1 等完全虚构，不是实测时延或实际 OpenIM 路由；渐进复杂度不是容量承诺。
- 下一章 02.10 从有限预算进入动态规划与贪心条件。
