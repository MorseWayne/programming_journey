# 14.05 审阅记录：提示与结构化输出

## 来源与生成状态

- 2026-09-25 对 DeepTutor BookEngine 发起八节中文深度课程生成；本地运行环境 `DEEPTUTOR_HOME=/srv/deeptutor` 已就绪，模型端点在 ideation 阶段连续九次返回 HTTP 502，没有生成 book、page 或原稿。
- 本目录是**人工编写并审阅的静态章稿归档**。`generation_request.json` 保存当次请求，`reviewed.md` 与正式页同步；没有 `original.md`，也不声称其由 DeepTutor 生成。端点恢复后单独生成、审阅差异并补充来源。

## 单章覆盖与教学审阅

- 从 14.09 六题的 actor、资料权限、current/proposed 和金动作开始，先定业务结果，再讲任务指令、问题、证据和输出要求，防止初学者把提示当授权。
- 先解释零示例、少示例和推理时上下文，再给与六题不同的 `dev-01` 排版示例；明确教学六题不是未见测试集，不能把它们的答案放进示例后宣称评测提升。
- 用真实 JSON 对象和 JSON Schema 讲 `required`、`enum`、`additionalProperties`，再把状态语义、引用支持、现行合同及权限独立为后续校验；`contract_version` 专指回答结构，不与 IM `/v1` 混同。
- 分层检查非法 JSON、缺字段、空答案、提议状态误用、越权输入和私有正文输出；说明格式错误有限重试、业务/权限错误不能靠补标点修复。
- 把提示模板、回答结构、资料状态和 IM HTTP 四条版本轴分开；第七节再用 `doc-r9` 注入、截掉 current 资料的场景解释提示边界。
- 22 题按术语 8、输出推演 8、业务决策 6 分层，答案与固定 S2 合同、R9 待审、`q-03/q-04` 的不同状态一致。

## 权威核对与边界

- Google Cloud 原始提示设计文档用于零/少示例与提示结构；RFC 8259 和 JSON Schema 官方文档用于语法、对象、枚举与重复键名；OWASP 原始资料用于不可信文档和提示注入边界。
- 没有运行课程练习、模型、Go/IM 服务或站点；示例结果全部为人工纸上标签。静态检查和来源散列另行验证。
- 后续 14.06 才系统讲分块、索引和检索，本章把证据视为应用已经按身份与状态筛出的输入。
